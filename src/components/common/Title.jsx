export const Title24 = ({ children, className }) => {
  return (
    <p
      className={`${className} flex flex-col text-[24px] text-white font-semibold`}
    >
      {children}
    </p>
  );
};
export const Title14 = ({ children, className }) => {
  return (
    <p className={`${className} flex flex-col text-sm text-white font-normal`}>
      {children}
    </p>
  );
};
export const Title20 = ({ children, className }) => {
  return (
    <p
      className={`${className} flex flex-col text-[20px] text-white font-bold`}
    >
      {children}
    </p>
  );
};
export const Title18 = ({ children, className }) => {
  return (
    <p
      className={`${className} flex flex-col text-[18px] text-white font-normal`}
    >
      {children}
    </p>
  );
};
export const Title12 = ({ children, className }) => {
  return (
    <p
      className={`${className} flex flex-col text-[12px] text-white font-normal`}
    >
      {children}
    </p>
  );
};
export const Title16 = ({ children, className }) => {
  return (
    <p
      className={`${className} flex flex-col text-base text-white font-normal`}
    >
      {children}
    </p>
  );
};
